<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textViewStyle">@style/AppTheme.TextViewStyle</item>
    </style>

    <style name="ControlButton" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/btn_up</item>
        <item name="android:textColor">@color/button_color</item>
        <item name="android:width">70dp</item>
        <item name="android:height">70dp</item>
        <item name="android:maxWidth">70dp</item>
        <item name="android:maxHeight">70dp</item>
    </style>

    <style name="ControlButtonDown" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/btn_down</item>
        <item name="android:textColor">@color/button_color</item>
        <item name="android:width">70dp</item>
        <item name="android:height">70dp</item>
        <item name="android:maxWidth">70dp</item>
        <item name="android:maxHeight">70dp</item>
    </style>

    <style name="AppTheme.TextViewStyle">
        <item name="android:fontFamily">sans-serif-thin</item>
        <item name="android:typeface">sans</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
